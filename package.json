{
  "name": "chatevo-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Chatevo WhatsApp Commerce Platform — Monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:merchant":   "npm run dev   --workspace=apps/merchant",
    "dev:admin":      "npm run dev   --workspace=apps/admin",
    "build:merchant": "npm run build --workspace=apps/merchant",
    "build:admin":    "npm run build --workspace=apps/admin",
    "lint":           "npm run lint  --workspaces --if-present",
    "typecheck":      "npm run typecheck --workspaces --if-present",
    "sync-env":       "node scripts/sync-env.mjs",
    "db:push":        "cd apps/merchant && npx drizzle-kit push",
    "db:studio":      "cd apps/merchant && npx drizzle-kit studio"
  },
  "devDependencies": {
    "typescript": "^5",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm":  ">=8.0.0"
  }
}
